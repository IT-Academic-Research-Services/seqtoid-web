# Throwaway: preview sandbox smoke-gate proof

Disposable PR. Exists only to stand up a preview sandbox from a branch that
contains the smoke-gate host fix, and prove the sandbox reaches Ready rather
than dying on the PreSync gate.

Close without merging once the sandbox is verified.
