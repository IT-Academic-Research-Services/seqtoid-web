# Sandbox pipeline e2e verification -- 2026-07-16

Throwaway marker for a demo sandbox. Do not merge.

Purpose: prove a per-PR preview sandbox can run a sample end to end --
upload -> S3 -> Step Functions -> Batch -> results -- now that the shared dev
SWIPE roles have been granted access to the preview samples bucket.

Before this, a sandbox sample failed 1.4s into the pipeline at PreprocessInput:
the sandbox uploaded to its own isolated bucket, which the shared dev pipeline
had never been told about.

Delete this branch once the run reaches SUCCEEDED.
